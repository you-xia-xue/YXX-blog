# 简述
## IoC原理
[详细](https://liaoxuefeng.com/books/java/spring/ioc/basic/index.html)  
简单说 
不new实例类，而是把Spring容器中实例化的Bean组件注入到类对象里  
​因为不用自己new类，而是由容器注入，所以控制权反转给了容器，这是“控制反转”（IoC）
​但是要自己预先配置Bean组件和Bean组件之间的依赖关系，所以也称“依赖注入”（DI）