# `System`类
``` java
System.out.println(a)
```
# `scanner`类
``` java
//此处建立一个Scanner类，这个类通过System.in对象构建
Scanner scanner = new Scanner(System.in);
//对实例使用方法,scanner.next.trim()。Enter键跳到下一行时，返回当前行的所有字符。
String name = scanner.nextLine.trim();
```
# `NumberFormat`类
# `math`类