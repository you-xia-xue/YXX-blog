**类名**：驼峰命名，每个单词首字母大写
**方法名/变量** 以小写字母开头，后续的单词驼峰命名（如果有）
**常量**:全大写
**源文件名**：必须和类名相同，保存时以类名作为文件名保存，文件名后缀为`.java`
**主函数入口**：`public static void main(String[] args)`
## 标识符（变量名，类名，方法名）
* 只能以字母、$、\_作为收
* 首字符后可以有数字
* 关键字不能作为标识符
* 标识符大小敏感
